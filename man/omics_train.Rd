% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omicstweezer.R
\name{omics_train}
\alias{omics_train}
\title{Train one OmicsTweezer model}
\usage{
omics_train(
  train_x,
  train_y,
  test_x,
  dims,
  drops,
  epochs = 128L,
  batch_size = 128L,
  device = c("auto", "cpu", "cuda"),
  cuda_index = NULL,
  learning_rate = 1e-04,
  loss_weight = 1,
  seed = 2021L,
  verbose = TRUE
)
}
\arguments{
\item{train_x}{Numeric matrix with samples in rows and genes in columns.}

\item{train_y}{Numeric matrix with samples in rows and cell types in columns.}

\item{test_x}{Numeric matrix with target-domain bulk samples in rows and
genes in columns.}

\item{dims}{Integer vector of length 4 specifying hidden-layer dimensions.}

\item{drops}{Numeric vector of length 4 specifying dropout probabilities.}

\item{epochs}{Integer scalar. Number of training epochs.}

\item{batch_size}{Integer scalar. Minibatch size.}

\item{device}{Character scalar. One of `"auto"`, `"cpu"`, or `"cuda"`.
If `"auto"`, CUDA is used when available, otherwise CPU is used.}

\item{cuda_index}{Optional integer scalar giving the CUDA device index to use
when `device = "cuda"`. CUDA indices are zero-based.}

\item{learning_rate}{Numeric scalar. Learning rate passed to Adam.}

\item{loss_weight}{Numeric scalar. Weight applied to the Wasserstein-style
domain adaptation loss.}

\item{seed}{Integer scalar random seed.}

\item{verbose}{Logical scalar. If `TRUE`, prints an epoch-level progress bar.}
}
\value{
A named list with components:
\describe{
  \item{encoder}{Trained torch encoder module.}
  \item{predictor}{Trained torch predictor module.}
  \item{history}{Data frame with epoch-level total, MSE, OT, and weighted OT
  losses.}
}
}
\description{
Trains one OmicsTweezer neural network with supervised cell-type proportion
prediction on simulated pseudobulks and a Wasserstein-style domain adaptation
term using real bulk samples.
}
\details{
Each batch contains source-domain simulated pseudobulks with known
proportions and target-domain real bulk profiles without labels. The training
objective combines supervised MSE loss with a domain adaptation term computed
from the encoded source and target embeddings.
}
\references{
TAPE project PyTorch implementation:
\url{https://github.com/poseidonchan/TAPE/blob/main/TAPE/model.py}
}
