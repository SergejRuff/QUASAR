% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quasar_importer_exporter.R
\name{quasar_h5_read_dataframe}
\alias{quasar_h5_read_dataframe}
\title{Read an anndata dataframe group (obs / var) into a data.frame}
\usage{
quasar_h5_read_dataframe(filename, name, index_as_column = FALSE)
}
\arguments{
\item{filename}{path to the .h5ad file.}

\item{name}{group name, e.g. \code{"obs"}, \code{"var"} or \code{"raw/var"}.}

\item{index_as_column}{keep the index as a column in addition to using it as
rownames. Default \code{FALSE} (cleaner colData / meta.data).}
}
\value{
a data.frame with rownames taken from the anndata index.
}
\description{
Handles the modern dataframe encoding (per-column categorical groups,
nullable/masked integer & boolean groups, string and numeric arrays) as well
as two legacy factor layouts (a shared \code{__categories} group and
\code{/uns/<col>_categories}). The index dataset becomes the rownames.
}
