% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quasar_importer_exporter.R
\name{quasar_h5adimport}
\alias{quasar_h5adimport}
\title{Import an .h5ad file as a Seurat or SingleCellExperiment object}
\usage{
quasar_h5adimport(
  filename,
  as = c("seurat", "sce"),
  use.raw = TRUE,
  load.X = TRUE,
  load.obsm = TRUE,
  assay = "RNA",
  verbose = TRUE
)
}
\arguments{
\item{filename}{path to the .h5ad file.}

\item{as}{one of \code{"seurat"} or \code{"sce"}; the object type to return.}

\item{use.raw}{logical, default \code{TRUE}. Use \code{adata.raw.X} /
\code{adata.raw.var} when present, otherwise \code{adata.X} / \code{adata.var}.}

\item{load.X}{logical, whether to load the expression matrix. If \code{FALSE}
an all-zero sparse matrix of the right shape is used (much faster).}

\item{load.obsm}{logical, whether to load \code{adata.obsm} as reduced
dimensions / DimReducs.}

\item{assay}{Seurat assay name to populate (ignored for \code{as = "sce"}).}

\item{verbose}{logical, print a start line and a summary block.}
}
\value{
a \code{Seurat} or \code{SingleCellExperiment} object.
}
\description{
A single entry point that reads adata.X (or adata.raw.X), adata.obs,
adata.var and adata.obsm and assembles either a \code{Seurat} or a
\code{SingleCellExperiment}. A missing \code{adata.raw} downgrades gracefully
to \code{adata.X} with a warning rather than failing.
}
\examples{
\dontrun{
## Round-trip on tiny data: write a small bulk matrix, then read it back.
f   <- tempfile(fileext = ".h5ad")
mat <- matrix(rpois(15 * 8, 2), nrow = 15,
              dimnames = list(paste0("gene", 1:15), paste0("cell", 1:8)))
quasar_h5adexporter(mat, f, bulk_orientation = "genes_x_samples")

sce <- quasar_h5adimport(f, as = "sce",    use.raw = FALSE)
seu <- quasar_h5adimport(f, as = "seurat", use.raw = FALSE)
}
}
