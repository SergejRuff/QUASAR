% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omicstweezer.R
\name{omics_tweezer}
\alias{omics_tweezer}
\title{Run the complete OmicsTweezer workflow}
\usage{
omics_tweezer(
  sc_data,
  real_bulk,
  real_bulk_obs = NULL,
  ot_weight = 1,
  scale_minmax = FALSE,
  samplenum = 5000L,
  n_cells_per_bulk = 500L,
  sparse = TRUE,
  variance_threshold = 0.98,
  scaler = c("ss", "mms", "none"),
  batch_size = 128L,
  epochs = 30L,
  learning_rate = 1e-04,
  seed = 2021L,
  celltype_col = "CellType",
  assay = "RNA",
  slot = "counts",
  verbose = TRUE,
  device = c("auto", "cpu", "cuda"),
  cuda_index = NULL
)
}
\arguments{
\item{sc_data}{A single-cell reference object. Supported inputs are a
`Seurat` object, a `SingleCellExperiment` object, a matrix, or a
`data.frame`.}

\item{real_bulk}{Numeric matrix or `data.frame` with genes in rows and real
bulk samples in columns.}

\item{real_bulk_obs}{Optional matrix or `data.frame` with ground-truth
cell-type proportions for real bulk samples. Used only for reporting in the
returned object.}

\item{ot_weight}{Numeric scalar. Weight applied to the Wasserstein-style
domain adaptation loss.}

\item{scale_minmax}{Logical scalar. If `TRUE`, applies an outer global
min-max scaling step to simulated and real bulk count matrices before
processing.}

\item{samplenum}{Integer scalar. Number of simulated pseudobulk samples.}

\item{n_cells_per_bulk}{Integer scalar. Target number of single cells per
simulated pseudobulk.}

\item{sparse}{Logical scalar. If `TRUE`, applies sparse perturbation during
pseudobulk simulation.}

\item{variance_threshold}{Numeric scalar. Fraction of genes used to define
the variance cutoff during processing.}

\item{scaler}{Character scalar specifying the per-sample scaling method. One
of `"ss"`, `"mms"`, or `"none"`.}

\item{batch_size}{Integer scalar. Minibatch size for training and prediction.}

\item{epochs}{Integer scalar. Number of training epochs for each architecture.}

\item{learning_rate}{Numeric scalar. Learning rate passed to Adam.}

\item{seed}{Integer scalar random seed.}

\item{celltype_col}{Character scalar giving the metadata column containing
cell-type labels for `Seurat` and `SingleCellExperiment` input.}

\item{assay}{Character scalar giving the assay name to extract from a
`Seurat` object.}

\item{slot}{Character scalar giving the assay slot or assay name to extract.}

\item{verbose}{Logical scalar. If `TRUE`, prints workflow progress messages.}

\item{device}{Character scalar. One of `"auto"`, `"cpu"`, or `"cuda"`.
If `"auto"`, CUDA is used when available, otherwise CPU is used.}

\item{cuda_index}{Optional integer scalar giving the CUDA device index to use
when `device = "cuda"`. CUDA indices are zero-based.}
}
\value{
A named list with components:
\describe{
  \item{pred}{Data frame of averaged predicted cell-type proportions. If
  `real_bulk_obs` is supplied, this component is itself a list containing
  `pred` and `ground_truth`.}
  \item{per_model}{Named list of predictions from the `m256`, `m512`, and
  `m1024` architectures.}
  \item{simudata}{Simulated pseudobulk data returned by [omics_simulate()].}
  \item{processed}{Processed matrices returned by [omics_process()].}
  \item{models}{Named list of trained OmicsTweezer models.}
  \item{loss_history}{Data frame containing epoch-level training losses for
  all architectures.}
}
}
\description{
Runs OmicsTweezer cell-type deconvolution from a single-cell reference and
real bulk expression profiles. The workflow simulates pseudobulks, processes
simulated and real bulk data, trains three neural network architectures, and
averages their predictions.
}
\details{
The ensemble uses three architectures: `m256`, `m512`, and `m1024`.
Predictions are generated independently for each architecture and then
averaged sample-wise.
}
