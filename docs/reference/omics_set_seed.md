# Set reproducible random seeds for OmicsTweezer

Sets both the R random seed and the torch random seed.

## Usage

``` r
omics_set_seed(seed = 42L)
```

## Arguments

- seed:

  Integer scalar random seed.

## Value

Invisibly returns \`seed\`.
